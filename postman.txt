Collection Name
Order Execution Engine

1️⃣ Health Check
Request
GET /

URL
http://localhost:4000/

Expected Response
{
  "message": "Order Execution Engine is running"
}


✔ Confirms server is up

2️⃣ Create Market Order (Basic)
Request
POST /api/orders/execute

URL
http://localhost:4000/api/orders/execute

Headers
Content-Type: application/json

Body (JSON)
{
  "tokenIn": "SOL",
  "tokenOut": "USDC",
  "amount": 10
}

Expected Response
{
  "message": "Order accepted and queued for execution",
  "orderId": "UUID"
}


📌 Save orderId → used for WebSocket

3️⃣ Create Market Order (Idempotency)
Request
POST /api/orders/execute

Headers
Content-Type: application/json
Idempotency-Key: test-key-123

Body
{
  "tokenIn": "SOL",
  "tokenOut": "USDC",
  "amount": 5
}

Send the SAME request twice
Expected

Same orderId both times

Second request returns immediately

✔ Demonstrates idempotency

4️⃣ Invalid Order (Validation Test)
Request
POST /api/orders/execute

Body
{
  "tokenIn": "SOL",
  "tokenOut": "SOL",
  "amount": -1
}

Expected Response
400 Bad Request


✔ Demonstrates validation

5️⃣ WebSocket – Order Status (Single Order)
In Postman → New → WebSocket Request
URL
ws://localhost:4000/ws?orderId=<ORDER_ID>

Expected Messages (streamed)
{ "status": "routing" }

{ "status": "building", "data": { "chosenDex": "raydium" } }

{ "status": "submitted" }

{
  "status": "confirmed",
  "txHash": "...",
  "executionPrice": 101.23
}


✔ Core requirement satisfied

6️⃣ WebSocket – Failure Case (Slippage)

Repeat order creation multiple times until one fails.

Expected WS message:

{
  "status": "failed",
  "error": "SLIPPAGE_EXCEEDED"
}


✔ Demonstrates error handling + retry exhaustion

7️⃣ Concurrent Orders (3–5 orders)
In Postman

Duplicate Create Order request

Send 3–5 times quickly

Open multiple WebSocket tabs

One WS per orderId

What evaluators should see:

Orders processed in parallel

Interleaved WS updates

Queue handling concurrency

✔ Meets concurrency requirement

8️⃣ Optional (Very Strong Signal)
Get Order Status (REST)

If you added:

GET /api/orders/:id/status


Show:

Active state (Redis)

Final state (Postgres)

📤 EXPORTING THE COLLECTION (IMPORTANT)

In Postman:

Click collection

Export

Format: v2.1

Save as:

order-execution-engine.postman_collection.json


Add it to your repo root or /docs.

✅ POSTMAN DELIVERABLE STATUS
Requirement	Status
Order creation	✅
Idempotency	✅
WebSocket lifecycle	✅
Concurrency	✅
Failure handling	✅
Postman collection	✅ DONE